/**
 * A shape contains multiple blocks
 */
(function(exports) {
    'use strict';
    var Shape = exports.Shape = function(options) {
        this.blocks = [
            [0,1]
            [2,3]
        ];
    }

    Shape.prototype.asd = function(x, min, max) {
    };

}(ea));